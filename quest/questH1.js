const myArray = [
    name:"A", proccessed:100, errors:5;
    name:"B", proccessed:80, errors:4;
    name:"C", proccessed:120, errors:12;
]
function calculateSuccessRate(proccessed, errors) {
    let result =proccessed - errors;
    let multiplication = proccessed * 100;
    let division = result / multiplication;
    Math.round() 
}